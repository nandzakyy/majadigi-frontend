services:
  frontend:
    build:
      context: .
      args:
        API_BASE_URL: ${API_BASE_URL:-http://localhost:3000}
    container_name: majadigi-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
